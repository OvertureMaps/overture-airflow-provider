# E2E tests

Reserved for live-platform tests against real Glue / Databricks / Wherobots
clusters. Tracked as a follow-up to the MVP; these need CI credentials and
test accounts in place before they can run.

The MVP suite ships with unit + mocked-SDK coverage only (see `tests/`).
